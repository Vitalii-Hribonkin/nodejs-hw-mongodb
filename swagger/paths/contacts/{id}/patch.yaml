tags:
  - Contacts
summary: Update contact by id
operationId: updateContact
description: Update specific contact by ID
security:
  - bearerAuth: []
parameters:
  - in: path
    name: id
    required: true
    schema:
      type: string
      example: 60af884ac45d3c001f1f5c7e
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        properties:
          name:
            type: string
          email:
            type: string # Прибираємо можливість null
            example: 'anna.kovalenko@example.com'
          phoneNumber:
            type: string
          isFavourite:
            type: boolean
          contactType:
            type: string
            enum: [home, personal]
          photo:
            type: string
            format: binary
responses:
  '200':
    description: Contact updated successfully
    content:
      application/json:
        schema:
          $ref: '../../../components/schemas/contact.yaml'
  '400':
    description: Invalid ID format or invalid request body
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: 400
            message:
              type: string
              example: Invalid contact ID or request body
  '401':
    $ref: '../../../components/responses/401.yaml'
  '404':
    $ref: '../../../components/responses/404.yaml'
